import Foundation

// TODO(Phase 1) — see CLAUDE.md / docs/PLAN.md
// Fetch Gcenx releases, download tar.xz to cache/, unpack into runners/<version>/, xattr -dr com.apple.quarantine, list installed, mark default.
