# Decanter
Lutris-style Wine game manager for macOS (personal build, unsigned).

```sh
softwareupdate --install-rosetta   # Apple Silicon only
swift build && swift run Decanter
```
Open this folder with `claude` to start; CLAUDE.md has the plan.
